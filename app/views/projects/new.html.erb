<!--CREATE a NEW project page-->

<div class="row">
  <div class="span6 offset3 center" >
    <img src= "http://www.un.org/africarenewal/sites/dr7.un.org.africarenewal/files/malian-women-create-beauty.jpg" alt="create">
    <h1>create a new project</h1>

    <% if signed_in? %>
      <%= form_for :project, url: projects_path do |f| %>
        <p>
          <%= f.label :title %><br>
          <%= f.text_field :title %>
        </p>
     
        <p>
         <%= f.label :location %><br>
          <%= f.text_field :location %>
        </p>

       <p>
          <%= f.label :description %><br>
          <%= f.text_area :description %>
        </p>
     
        <p>
          <%= f.submit %>
        </p>
    <% end %>

  <% else %>
    <h3> <%= link_to "sign in", signin_path %> or <%= link_to "sign up", signup_path %> to create a new project! </h3>

  <% end %>
  </div>
</div>

